#include <string>
#include <iostream>

using namespace std;
namespace handelsPlatz{

    class handelsplatz
    {
    private:

    public:
        handelsplatz(/* args */);
        bool kaufen();
        bool verkaufen();
        bool login(string name, int password);
        int handelswerteAbrufen();



    };
    
  

}